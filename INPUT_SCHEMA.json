{
    "title": "Jobs.ch SERP Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "List of Jobs.ch search URLs to scrape",
            "editor": "stringList"
        },
        "maxPages": {
            "title": "Maximum Pages",
            "type": "integer",
            "description": "Maximum number of pages to scrape per search URL",
            "minimum": 1,
            "maximum": 100,
            "default": 10
        },
        "filterRecruitingRoles": {
            "title": "Filter Recruiting Roles",
            "type": "boolean",
            "description": "Only scrape jobs related to recruiting/HR",
            "default": true
        },
        "maxConcurrency": {
            "title": "Maximum Concurrency",
            "type": "integer",
            "description": "Maximum number of concurrent requests",
            "minimum": 1,
            "maximum": 10,
            "default": 3
        },
        "useApifyProxy": {
            "title": "Use Apify Proxy",
            "type": "boolean",
            "description": "Whether to use Apify's proxy service",
            "default": true
        }
    },
    "required": ["startUrls"]
} 